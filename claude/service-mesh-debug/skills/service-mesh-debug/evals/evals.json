{
  "skill_name": "fix-flaky-e2e",
  "evals": [
    {
      "id": 1,
      "prompt": "This test is flaky in CI - it passes locally but fails maybe 1 in 5 runs. Can you diagnose and fix it?\n\n```go\npackage gateway_test\n\nimport (\n\t. \"github.com/onsi/ginkgo/v2\"\n\t. \"github.com/onsi/gomega\"\n\n\t. \"github.com/kumahq/kuma/v2/test/framework\"\n\t\"github.com/kumahq/kuma/v2/test/framework/client\"\n)\n\nvar _ = Describe(\"Gateway connectivity\", func() {\n\tIt(\"should route traffic through the gateway\", func() {\n\t\t// Apply MeshGateway\n\t\tExpect(k8sCluster.Install(gatewaySetup)).To(Succeed())\n\n\t\t// Wait for gateway pod\n\t\tEventually(func() bool {\n\t\t\tpod, err := PodNameOfApp(k8sCluster, \"gateway\", \"default\")\n\t\t\tif err != nil {\n\t\t\t\treturn false\n\t\t\t}\n\t\t\treturn pod != \"\"\n\t\t}, \"30s\", \"1s\").Should(BeTrue())\n\n\t\t// Assert traffic routes correctly\n\t\tEventually(func() {\n\t\t\tresp, err := client.CollectEchoResponse(\n\t\t\t\tk8sCluster, \"demo-client\", \"http://gateway.default/\",\n\t\t\t\tclient.FromKubernetesPod(\"default\", \"demo-client\"),\n\t\t\t)\n\t\t\tExpect(err).ToNot(HaveOccurred())\n\t\t\tExpect(resp.Instance).To(Equal(\"backend\"))\n\t\t}, \"30s\", \"1s\").Should(Succeed())\n\t})\n})\n```",
      "expected_output": "Identify multiple flakiness issues: (1) bare Expect inside Eventually (no g Gomega), (2) short 30s timeout for gateway traffic - should be 60s, (3) no AfterEachFailure debug hook. Apply all fixes."
    },
    {
      "id": 2,
      "prompt": "Hey the test at test/e2e/resilience/resilience_multizone_k8s.go keeps failing in CI when we restart a zone CP. The test kills the zone CP pod and then immediately tries to get the pod name of the test app. Here's the flaky bit:\n\n```go\nIt(\"should recover after zone CP restart\", func() {\n\t// Kill the zone CP\n\tpods := zone1.GetKuma().(*K8sControlPlane).GetKumaCPPods()\n\tExpect(pods).To(HaveLen(1))\n\terr := k8s.RunKubectlE(zone1.GetTesting(), zone1.GetKubectlOptions(),\n\t\t\"delete\", \"pod\", pods[0].GetName(), \"-n\", pods[0].GetNamespace())\n\tExpect(err).ToNot(HaveOccurred())\n\n\t// Kill and restart the test app too\n\tExpect(zone1.DeletePod(\"test-server\", TestNamespace)).To(Succeed())\n\n\t// Get the new pod name and verify it works\n\tnewPod, err := PodNameOfApp(zone1, \"test-server\", TestNamespace)\n\tExpect(err).ToNot(HaveOccurred())\n\n\t// Verify connectivity restored\n\tEventually(func(g Gomega) {\n\t\tresp, err := client.CollectEchoResponse(zone1, \"demo-client\",\n\t\t\t\"http://test-server/\",\n\t\t\tclient.FromKubernetesPod(TestNamespace, \"demo-client\"))\n\t\tg.Expect(err).ToNot(HaveOccurred())\n\t\tg.Expect(resp.Instance).To(Equal(\"test-server\"))\n\t}, \"30s\", \"1s\").Should(Succeed())\n})\n```",
      "expected_output": "Identify PodNameOfApp race after DeletePod (terminating pod still listed). Fix: wrap PodNameOfApp in Eventually. Also flag 30s timeout is short for cross-zone recovery (should be 2m)."
    },
    {
      "id": 3,
      "prompt": "our mTLS test keeps failing in ci with 'connection refused' or tls handshake errors right after we deploy a new workload. locally it always passes. here's the test:\n\n```go\nvar _ = Describe(\"mTLS between services\", Ordered, func() {\n\tBeforeAll(func() {\n\t\tExpect(NewClusterSetup().\n\t\t\tInstall(MTLSMeshKubernetes(\"default\")).\n\t\t\tInstall(testserver.Install(\n\t\t\t\ttestserver.WithName(\"backend\"),\n\t\t\t\ttestserver.WithNamespace(\"test-ns\"),\n\t\t\t)).\n\t\t\tInstall(democlient.Install(\n\t\t\t\tdemoclient.WithName(\"client\"),\n\t\t\t\tdemoclient.WithNamespace(\"test-ns\"),\n\t\t\t)).\n\t\t\tSetup(KubeCluster)).To(Succeed())\n\t})\n\n\tIt(\"should connect with mTLS\", func() {\n\t\tresp, err := client.CollectEchoResponse(\n\t\t\tKubeCluster, \"client\", \"http://backend.test-ns.svc.cluster.local/\",\n\t\t\tclient.FromKubernetesPod(\"test-ns\", \"client\"),\n\t\t)\n\t\tExpect(err).ToNot(HaveOccurred())\n\t\tExpect(resp.Instance).To(Equal(\"backend\"))\n\t})\n})\n```",
      "expected_output": "Identify missing readiness gate: traffic asserted immediately after Setup without waiting for xDS/SDS to deliver mTLS certs (dynamic_warming_secrets). Fix: wrap connectivity assertion in Eventually with 2m timeout, or add explicit wait for DP readiness before the It block."
    }
  ]
}
