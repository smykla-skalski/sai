interface:
  display_name: "Promptgen"
  short_description: "Turn rough asks into strong prompts"
  default_prompt: "Use $promptgen to turn this rough instruction into a stronger prompt with a clear outcome contract, model fit, and verification rules."

policy:
  allow_implicit_invocation: true
